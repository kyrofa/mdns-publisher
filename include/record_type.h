#ifndef RECORD_TYPE_H
#define RECORD_TYPE_H

// There are approximately one bajillion types, so we'll just list
// what we support, here.
enum RecordType
{
	INVALID = 0,
	A = 1,
	AAAA = 28
};

#endif // RECORD_TYPE_H
